---
# Executed with the inventory created by the playbook.
#   ansible-playbook -i cluster.status<cluster_job> clean.yml
#
- hosts: localhost
  connection: local
  gather_facts: False
  roles:
    - clean
